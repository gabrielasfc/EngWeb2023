extends layout

block content
  .w3-card-4
    header.w3-container.w3-teal
      h1 Aluno #{a.id}
    .w3-container
        ul.w3-ul.w3-card-4(style="width:50%")
            li #[b Nome: ] #{a.nome}
            li #[b Número: ] #{a.id}
            li #[b Git: ] #[a(href="#{a.gitlink}") #{a.gitlink}] 
        fieldset
            each i in [1, 2, 3, 4, 5, 6, 7, 8]
                - var tpc = "tpc" + i
                - var isChecked = tpc in a
                label TPC#{i}
                    input.w3-check(type="checkbox", name=tpc, value="1", checked=isChecked, disabled)
    footer.w3-container.w3-teal
      address Generated by RPCW2023 in #{d} - #[a(href="/") Voltar]